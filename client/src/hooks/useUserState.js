// set localstorage flag to true

const userState = () => {

    // in local storage
    // set flag to true so that indexDark.css is fired
    // set username to username so that username is passed as props to myprofile
    // set ability to ability so that ability is passed as props to myprofile
    // set id to id so that user id is saved in localstorage and can be used on any page to update user stats


}